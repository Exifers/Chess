#include "motionComputer.hh"

bool
MotionComputer::isDoable(Piece *piece, struct Chess::move motion) {
  piece = piece;
  motion = motion;
  return true;
}

bool
MotionComputer::isDoableKnight(struct Chess::move motion) {
  motion = motion;
  return true;
}

bool
MotionComputer::isDoablePawn(Piece *piece, struct Chess::move motion) {
  piece = piece;
  motion = motion;
  return true;
}

bool
MotionComputer::isDoableGeneric(Piece *piece, struct Chess::move motion) {
  piece = piece;
  motion = motion;
  return true;
}
